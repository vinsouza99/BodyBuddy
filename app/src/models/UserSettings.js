export default class UserSettings {
  /**
   *
   * @param {Intensity} intensity
   * @param {Goal} goal
   */
  constructor(goal, intensity) {
    this.goal = goal;
    this.intensity = intensity;
  }
}
